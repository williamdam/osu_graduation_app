class PostDTO {
  
  PostDTO({this.firstName, this.lastName, this.email, this.message, this.filepath});
  
  String firstName;
  String lastName;
  String email;
  String message;
  String filepath;
}